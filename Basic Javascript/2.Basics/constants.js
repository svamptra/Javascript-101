const interestRates = 0.3;
interestRates = 1;
console.log(interestRates); 

// The value of a variable can change, but the value of a constant cannot change.


/*
Best practice:

    if you don't want to reassign, constant(cosnt) should be your default choice.
    Otherwise if you need to reassign a variable use let.
 */