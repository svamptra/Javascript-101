// Every object in javascript except the root object has a prototype

// constructors also have a prototype property

function Circle(radius) {
    this.radius = radius;
}
Circle.prototype
const circle = new Circle(1);